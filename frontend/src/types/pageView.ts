// Enum corresponding to which view is visible UNDER the ControlPanel
enum pageView {
    MAIN,        // main view, which is List of StripeItems
    SETTINGS     // settings view
}

export { pageView }